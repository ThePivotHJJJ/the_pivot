<div class="row text-center row">
  <%= render partial: "partials/sidebar" %>

  <div class="col-md-4 col-xs-12">
    <h1>your_orders</h1>
      <% @orders.each do |order| %>
        <h4> <%= link_to "order_#{order.id}", order_path(order) %> </h4>
      <% end %>
    </div>
  </div>
</div>
