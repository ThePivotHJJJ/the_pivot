<div class="container">
  <div class="gifs" id="item-list">
   <div class="row text-center">
      <h1>All Items</h1>
      <% @items.each do |item| %>
        <div id="item">
          <%= link_to item.name, item_path(item) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
